"""
Make `apps.albums` an explicit Python package.

This file is intentionally empty; it ensures imports and test collection
use the full package path `apps.albums.tests` instead of a bare `tests`.
"""
