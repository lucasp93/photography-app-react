"""
Make `apps.photos` an explicit Python package.

This file is intentionally empty; it ensures imports and test collection
use the full package path `apps.photos.tests` instead of a bare `tests`.
"""
