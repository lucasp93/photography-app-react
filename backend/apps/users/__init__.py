"""
Make `apps.users` an explicit Python package.

This file is intentionally empty; it ensures imports and test collection
use the full package path `apps.users.tests` instead of a bare `tests`.
"""
