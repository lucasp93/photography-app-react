"""
Make `apps` an explicit Python package.

This file is intentionally empty; it ensures imports like
`from apps.orders.models import Order` resolve consistently during
test collection and runtime.
"""
